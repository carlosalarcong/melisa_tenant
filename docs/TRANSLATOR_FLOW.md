# 🔄 Flujo Completo de TranslatorInterface en melisa_tenant

## Diagrama de Arquitectura Completa

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          1. REQUEST LLEGA                               │
│     GET https://melisahospital.melisaupgrade.prod/dashboard            │
│     Header: Accept-Language: es-CL,es;q=0.9,en;q=0.8                   │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                   2. LocaleListener (Priority 20)                       │
│     KernelEvents::REQUEST ejecuta ANTES de los controllers             │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│              3. LocalizationService::getCurrentLocale()                 │
│                                                                         │
│   ┌─── NIVEL 1: ¿Hay _locale en sesión? ────────────┐                 │
│   │    Session::get('_locale')                       │                 │
│   │    ✅ SI → return 'es'                           │                 │
│   │    ❌ NO → Siguiente nivel                       │                 │
│   └──────────────────────────────────────────────────┘                 │
│                             ↓                                           │
│   ┌─── NIVEL 2: ¿Tenant tiene locale? ──────────────┐                 │
│   │    TenantContext::getCurrentTenant()['locale']   │                 │
│   │    ✅ SI → return 'en'                           │                 │
│   │    ❌ NO → Siguiente nivel                       │                 │
│   └──────────────────────────────────────────────────┘                 │
│                             ↓                                           │
│   ┌─── NIVEL 3: ¿Header Accept-Language válido? ────┐                 │
│   │    Request::getPreferredLanguage(['es', 'en'])   │                 │
│   │    ✅ SI → return 'es'                           │                 │
│   │    ❌ NO → Siguiente nivel                       │                 │
│   └──────────────────────────────────────────────────┘                 │
│                             ↓                                           │
│   ┌─── NIVEL 4: Fallback ─────────────────────────── ┐                 │
│   │    return 'es' (default)                         │                 │
│   └──────────────────────────────────────────────────┘                 │
│                                                                         │
│   Resultado: $locale = 'es'                                            │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    4. Request::setLocale('es')                          │
│     Establece el locale en el Request de Symfony                        │
│     Session::set('_locale', 'es')  → Persistencia                       │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│         5. DynamicControllerSubscriber (Priority 15) ejecuta            │
│     Resuelve controller específico del tenant                           │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│           6. TenantContextInjector (Priority 10) ejecuta                │
│     Inyecta TenantContext en controller via Reflection                  │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                      7. CONTROLLER EJECUTA                              │
│                                                                         │
│   class DefaultController extends AbstractTenantAwareController         │
│   {                                                                     │
│       public function index(): Response                                 │
│       {                                                                 │
│           return $this->render('dashboard/index.html.twig', [          │
│               'tenant_name' => $this->getTenantName()                   │
│           ]);                                                           │
│       }                                                                 │
│   }                                                                     │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                      8. TEMPLATE RENDERIZA                              │
│                                                                         │
│   dashboard/index.html.twig:                                            │
│                                                                         │
│   <h1>{{ 'dashboard.title'|trans }}</h1>                                │
│   <p>{{ 'dashboard.welcome'|trans({'%name%': user.name}) }}</p>        │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                   9. TWIG TRANSLATION EXTENSION                         │
│                                                                         │
│   TranslationExtension::trans('dashboard.title', [], 'messages')        │
│   │                                                                     │
│   └─→ Obtiene locale del Request: 'es'                                 │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│              10. TranslatorInterface::trans() EJECUTA                   │
│                                                                         │
│   Symfony\Component\Translation\Translator::trans(                      │
│       'dashboard.title',    // $id                                      │
│       [],                   // $parameters                              │
│       'messages',           // $domain                                  │
│       'es'                  // $locale                                  │
│   )                                                                     │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                11. MessageCatalogue::get() BUSCA                        │
│                                                                         │
│   ¿Existe catálogo en memoria para 'es'?                               │
│   ❌ NO → Cargar desde filesystem                                       │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                   12. CARGAR ARCHIVO DE TRADUCCIÓN                      │
│                                                                         │
│   ¿Existe caché compilada?                                             │
│   ✅ SÍ → Cargar desde:                                                 │
│      var/cache/dev/translations/catalogue.es.messages.php               │
│                                                                         │
│   ❌ NO → Parsear YAML:                                                 │
│      translations/messages.es.yaml                                      │
│      │                                                                  │
│      ├─ dashboard:                                                      │
│      │    title: 'Tablero de Control'                                  │
│      │    welcome: 'Bienvenido, %name%'                                │
│      │                                                                  │
│      └─ Convertir a Array PHP y cachear                                │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                13. MessageCatalogue EN MEMORIA                          │
│                                                                         │
│   catalogues['es']['messages'] = [                                      │
│       'dashboard.title' => 'Tablero de Control',                        │
│       'dashboard.welcome' => 'Bienvenido, %name%',                      │
│       'auth.login' => 'Iniciar Sesión',                                 │
│       'auth.username' => 'Usuario',                                     │
│       ... (200+ claves más)                                             │
│   ]                                                                     │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                      14. BUSCAR CLAVE                                   │
│                                                                         │
│   $message = catalogues['es']['messages']['dashboard.title']           │
│   $message = 'Tablero de Control'                                      │
│                                                                         │
│   ¿Hay parámetros para reemplazar?                                     │
│   ❌ NO → Retornar directamente                                         │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                  15. RETORNAR AL TEMPLATE                               │
│                                                                         │
│   return 'Tablero de Control'                                           │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                16. TWIG RENDERIZA HTML FINAL                            │
│                                                                         │
│   <h1>Tablero de Control</h1>                                           │
│   <p>Bienvenido, Juan Pérez</p>                                        │
│                                                                         │
└────────────────────────────┬────────────────────────────────────────────┘
                             │
                             ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    17. RESPONSE AL NAVEGADOR                            │
│                                                                         │
│   HTTP/1.1 200 OK                                                       │
│   Content-Type: text/html; charset=UTF-8                                │
│   Content-Language: es                                                  │
│                                                                         │
│   <html>                                                                │
│       <h1>Tablero de Control</h1>                                       │
│       <p>Bienvenido, Juan Pérez</p>                                    │
│   </html>                                                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Componentes y su Rol

| Componente | Responsabilidad | Prioridad |
|------------|-----------------|-----------|
| **TenantConnectionListener** | Cambiar BD según tenant | 1000 (más alta) |
| **DynamicControllerSubscriber** | Resolver controller dinámico | 15 |
| **TenantContextInjector** | Inyectar TenantContext | 10 |
| **LocaleListener** | Establecer locale | 20 |
| **LocalizationService** | Lógica multi-nivel de locale | - |
| **TranslatorInterface** | Motor de traducciones | - |
| **MessageCatalogue** | Caché en memoria | - |
| **YAML Files** | Almacenamiento persistente | - |

---

## 🔄 Flujo Simplificado

```
Request 
  → LocaleListener 
    → LocalizationService (4 niveles) 
      → setLocale('es')
        → Controller ejecuta
          → Template: {{ 'key'|trans }}
            → TranslatorInterface
              → MessageCatalogue
                → YAML File
                  → Traducción
                    → HTML Response
```

---

## ⚡ Optimizaciones de Performance

### 1️⃣ Caché en Memoria (MessageCatalogue)
```php
// Primera traducción del request:
{{ 'auth.login'|trans }}  // → Lee translations/messages.es.yaml (lento)

// Segunda traducción del mismo request:
{{ 'auth.password'|trans }}  // → Usa MessageCatalogue (rápido)
```

### 2️⃣ Caché Compilada (Filesystem)
```php
// Primer request (dev):
Translator → Lee YAML → Parsea → Almacena en var/cache/

// Segundo request (dev):
Translator → Lee var/cache/catalogue.es.messages.php (PHP compilado)

// Producción:
Translator → SIEMPRE usa caché compilada (súper rápido)
```

### 3️⃣ Lazy Loading
```php
// Translator NO carga todos los idiomas al iniciar
// Solo carga el idioma cuando se usa por primera vez

Request con locale='es' → Solo carga messages.es.yaml
Request con locale='en' → Solo carga messages.en.yaml
```

---

## 🎯 Ejemplo Completo con Parámetros

### Template Twig
```twig
{{ 'auth.user_not_found'|trans({'%tenant%': tenant_name}) }}
```

### Flujo Interno
```
1. Twig Extension ejecuta:
   trans('auth.user_not_found', ['%tenant%' => 'Hospital'])

2. TranslatorInterface::trans():
   - Busca en MessageCatalogue['es']['messages']['auth.user_not_found']
   - Encuentra: "Usuario no encontrado en %tenant%"
   
3. Reemplazar parámetros:
   - str_replace('%tenant%', 'Hospital', "Usuario no encontrado en %tenant%")
   - Resultado: "Usuario no encontrado en Hospital"
   
4. Retornar al template

5. HTML final:
   <p>Usuario no encontrado en Hospital</p>
```

---

## 🌐 Traducciones Específicas por Tenant

### LocalizationService::getTenantSpecificTranslations()

```php
// Hospital:
$translations['melisahospital']['es'] = [
    'establishment_type' => 'Hospital',
    'main_service' => 'Atención Hospitalaria'
];

// Clínica:
$translations['melisalacolina']['es'] = [
    'establishment_type' => 'Clínica',
    'main_service' => 'Atención Clínica Especializada'
];
```

Estas NO están en TranslatorInterface, son **dinámicas** y específicas del tenant.

---

## 🔑 Puntos Clave

1. **TranslatorInterface** es el motor core de Symfony
2. **LocalizationService** lo envuelve con lógica multi-tenant
3. **LocaleListener** establece el locale ANTES de controllers
4. **MessageCatalogue** cachea traducciones en memoria por request
5. **Filesystem Cache** cachea YAML compilado entre requests
6. **Traducciones específicas** por tenant son dinámicas (no en YAML)

🌍 **Sistema completo y optimizado para multi-tenancy!**
