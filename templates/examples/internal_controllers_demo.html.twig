{% extends 'base.html.twig' %}

{% block title %}Example: Internal Controllers - {{ tenant_info.name ?? 'Melisa Hospital' }}{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    
    <!-- Header -->
    <div class="mb-8">
        <h1 class="text-4xl font-bold text-gray-800 mb-2">
            🎮 Demo: Controllers Internos
        </h1>
        <p class="text-gray-600">
            Ejemplo de uso de controllers internos (no API Platform) con sistema de fallback
        </p>
        <div class="mt-2 flex gap-2">
            <div class="inline-flex items-center px-3 py-1 rounded-full text-sm bg-blue-100 text-blue-800">
                📡 Subdomain: <strong class="ml-1">{{ tenant_info.subdomain ?? 'melisahospital' }}</strong>
            </div>
            <div class="inline-flex items-center px-3 py-1 rounded-full text-sm bg-green-100 text-green-800">
                🎮 Controller: internal--patient
            </div>
        </div>
    </div>

    <!-- Formulario de Paciente Interno -->
    <div data-controller="internal--patient"
         data-internal--patient-validate-on-change-value="true"
         data-internal--patient-auto-save-value="false"
         data-internal--patient-is-editing-value="false"
         data-internal--patient-required-fields-value='["name", "cedula"]'
         class="bg-white rounded-lg shadow-lg p-8">

        <h2 class="text-2xl font-semibold mb-6 text-gray-800">
            Formulario de Paciente
        </h2>

        <!-- Mensajes -->
        <div data-internal--patient-target="loading" class="hidden bg-blue-100 border border-blue-400 text-blue-700 px-4 py-3 rounded mb-4">
            Guardando...
        </div>
        
        <div data-internal--patient-target="error" class="hidden bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        </div>
        
        <div data-internal--patient-target="success" class="hidden bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
        </div>

        <!-- Formulario -->
        <form data-internal--patient-target="form" data-action="submit->internal--patient#submitForm">
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                
                <!-- Información Personal -->
                <div class="md:col-span-2">
                    <h3 class="text-lg font-semibold text-gray-700 mb-4 border-b pb-2">
                        📋 Información Personal
                    </h3>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        Nombre Completo *
                    </label>
                    <input type="text" 
                           data-internal--patient-target="name"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                           placeholder="Ingrese nombre completo"
                           required>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        Cédula *
                    </label>
                    <input type="text" 
                           data-internal--patient-target="cedula"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                           placeholder="12345678"
                           pattern="[0-9]{7,8}"
                           required>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        Email
                    </label>
                    <input type="email" 
                           data-internal--patient-target="email"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                           placeholder="paciente@email.com">
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        Teléfono
                    </label>
                    <input type="tel" 
                           data-internal--patient-target="phone"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                           placeholder="+56 9 xxxx xxxx">
                </div>

                <div class="md:col-span-2">
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        Dirección
                    </label>
                    <input type="text" 
                           data-internal--patient-target="address"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
                           placeholder="Dirección completa">
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        Género
                    </label>
                    <select data-internal--patient-target="gender"
                            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <option value="">Seleccionar...</option>
                        <option value="M">Masculino</option>
                        <option value="F">Femenino</option>
                        <option value="O">Otro</option>
                    </select>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        Fecha de Nacimiento
                    </label>
                    <input type="date" 
                           data-internal--patient-target="birthDate"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                </div>

                <!-- Información Médica -->
                <div class="md:col-span-2 mt-6">
                    <h3 class="text-lg font-semibold text-gray-700 mb-4 border-b pb-2">
                        🩺 Información Médica
                    </h3>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        Tipo de Sangre
                    </label>
                    <select data-internal--patient-target="bloodType"
                            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <option value="">Seleccionar...</option>
                        <option value="A+">A+</option>
                        <option value="A-">A-</option>
                        <option value="B+">B+</option>
                        <option value="B-">B-</option>
                        <option value="AB+">AB+</option>
                        <option value="AB-">AB-</option>
                        <option value="O+">O+</option>
                        <option value="O-">O-</option>
                    </select>
                </div>

                <!-- Campos específicos según el subdomain -->
                {% if tenant_info.subdomain == 'melisalacolina' %}
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        🏥 Especialidad
                    </label>
                    <select data-internal--melisalacolina--patient-target="specialty"
                            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-green-500"
                            data-action="change->internal--melisalacolina--patient#changeSpecialty">
                        <!-- Se llena dinámicamente por el controller -->
                    </select>
                </div>

                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">
                        💳 Seguro Médico
                    </label>
                    <select data-internal--melisalacolina--patient-target="insuranceProvider"
                            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-green-500">
                        <option value="">Seleccionar...</option>
                        <option value="fonasa-a">Fonasa Grupo A</option>
                        <option value="fonasa-b">Fonasa Grupo B</option>
                        <option value="isapre-banmedica">Isapre Banmédica</option>
                        <option value="isapre-colmena">Isapre Colmena</option>
                    </select>
                </div>
                {% endif %}

            </div>

            <!-- Botones -->
            <div class="mt-8 flex gap-4">
                <button type="submit" 
                        data-internal--patient-target="submitButton"
                        class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-md font-medium transition-colors">
                    💾 Guardar Paciente
                </button>
                
                <button type="button" 
                        data-internal--patient-target="cancelButton"
                        data-action="click->internal--patient#cancel"
                        class="bg-gray-500 hover:bg-gray-600 text-white px-6 py-2 rounded-md font-medium transition-colors">
                    ❌ Cancelar
                </button>
                
                <button type="button" 
                        data-action="click->internal--patient#clearForm"
                        class="bg-red-500 hover:bg-red-600 text-white px-6 py-2 rounded-md font-medium transition-colors">
                    🗑️ Limpiar
                </button>
            </div>

        </form>

        <!-- Info de Debug -->
        <div class="mt-8 p-4 bg-gray-100 rounded-md">
            <h4 class="font-semibold text-gray-700 mb-2">🐛 Debug Info:</h4>
            <ul class="text-sm text-gray-600 space-y-1">
                <li><strong>Controller usado:</strong> internal--patient</li>
                <li><strong>Fallback:</strong> {{ tenant_info.subdomain ?? 'melisahospital' }} → default</li>
                <li><strong>Validación:</strong> Tiempo real habilitada</li>
                <li><strong>Auto-save:</strong> Deshabilitado</li>
            </ul>
        </div>

    </div>

    <!-- Info del Sistema -->
    <div class="mt-8 bg-blue-50 border border-blue-200 rounded-lg p-6">
        <h3 class="text-lg font-semibold text-blue-800 mb-3">
            📖 Cómo funciona el sistema de fallback
        </h3>
        <div class="text-blue-700 space-y-2">
            <p><strong>1. Búsqueda por subdomain:</strong> Busca <code>internal/{{ tenant_info.subdomain ?? 'melisahospital' }}/patient_controller.js</code></p>
            <p><strong>2. Fallback a default:</strong> Si no existe, usa <code>internal/default/patient_controller.js</code></p>
            <p><strong>3. Herencia:</strong> Controllers específicos extienden el default con funcionalidades extra</p>
            <p><strong>4. API Platform:</strong> Misma lógica para <code>apiplatform--api-patient</code></p>
        </div>
    </div>

</div>
{% endblock %}