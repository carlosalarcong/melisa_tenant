{% extends 'base.html.twig' %}

{% block title %}Examples - Stimulus Controllers - {{ tenant_info.name ?? 'Melisa Hospital' }}{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8">
    
    <!-- Header -->
    <div class="mb-8">
        <h1 class="text-4xl font-bold text-gray-800 mb-2">
            📚 Examples & Demos
        </h1>
        <p class="text-gray-600">
            Ejemplos interactivos del sistema de controllers Stimulus multi-tenant
        </p>
        <div class="mt-2 flex gap-2">
            <div class="inline-flex items-center px-3 py-1 rounded-full text-sm bg-blue-100 text-blue-800">
                📡 Tenant: <strong class="ml-1">{{ tenant_info.subdomain ?? 'melisahospital' }}</strong>
            </div>
            <div class="inline-flex items-center px-3 py-1 rounded-full text-sm bg-green-100 text-green-800">
                🎮 Stimulus 3.2.2
            </div>
            <div class="inline-flex items-center px-3 py-1 rounded-full text-sm bg-purple-100 text-purple-800">
                🚀 API Platform 4.2
            </div>
        </div>
    </div>

    <!-- Grid de Examples -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        
        <!-- API Platform Demo -->
        <div class="bg-white rounded-lg shadow-lg overflow-hidden">
            <div class="bg-gradient-to-r from-blue-500 to-blue-600 text-white p-6">
                <h2 class="text-2xl font-bold mb-2">
                    🚀 API Platform Integration
                </h2>
                <p class="text-blue-100">
                    Demo de integración con API Platform usando controllers especializados por tenant
                </p>
            </div>
            
            <div class="p-6">
                <div class="space-y-4 mb-6">
                    <div class="flex items-center text-gray-700">
                        <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">✓</span>
                        Controllers dinámicos con fallback automático
                    </div>
                    <div class="flex items-center text-gray-700">
                        <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">✓</span>
                        State Providers multi-tenant
                    </div>
                    <div class="flex items-center text-gray-700">
                        <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">✓</span>
                        Búsqueda en tiempo real y cache local
                    </div>
                    <div class="flex items-center text-gray-700">
                        <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">✓</span>
                        Especialización por clínica (La Colina, Wi Clinic)
                    </div>
                </div>
                
                <div class="bg-gray-50 rounded-lg p-4 mb-6">
                    <h4 class="font-semibold text-gray-700 mb-2">🎮 Controller usado:</h4>
                    <code class="text-sm text-blue-600 bg-blue-50 px-2 py-1 rounded">apiplatform--api-patient</code>
                    <p class="text-sm text-gray-600 mt-2">
                        Busca: <code>apiplatform/{{ tenant_info.subdomain ?? 'melisahospital' }}/</code> → <code>apiplatform/default/</code>
                    </p>
                </div>
                
                <a href="{{ path('examples_api_platform') }}" 
                   class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-4 rounded-lg transition-colors inline-block text-center">
                    🚀 Ver Demo API Platform
                </a>
            </div>
        </div>

        <!-- Internal Controllers Demo -->
        <div class="bg-white rounded-lg shadow-lg overflow-hidden">
            <div class="bg-gradient-to-r from-green-500 to-green-600 text-white p-6">
                <h2 class="text-2xl font-bold mb-2">
                    🎮 Internal Controllers
                </h2>
                <p class="text-green-100">
                    Demo de controllers internos para formularios y UI sin API Platform
                </p>
            </div>
            
            <div class="p-6">
                <div class="space-y-4 mb-6">
                    <div class="flex items-center text-gray-700">
                        <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">✓</span>
                        Validación en tiempo real
                    </div>
                    <div class="flex items-center text-gray-700">
                        <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">✓</span>
                        Auto-save opcional
                    </div>
                    <div class="flex items-center text-gray-700">
                        <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">✓</span>
                        Campos específicos por clínica
                    </div>
                    <div class="flex items-center text-gray-700">
                        <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">✓</span>
                        Manejo de errores robusto
                    </div>
                </div>
                
                <div class="bg-gray-50 rounded-lg p-4 mb-6">
                    <h4 class="font-semibold text-gray-700 mb-2">🎮 Controller usado:</h4>
                    <code class="text-sm text-green-600 bg-green-50 px-2 py-1 rounded">internal--patient</code>
                    <p class="text-sm text-gray-600 mt-2">
                        Busca: <code>internal/{{ tenant_info.subdomain ?? 'melisahospital' }}/</code> → <code>internal/default/</code>
                    </p>
                </div>
                
                <a href="{{ path('examples_internal_controllers') }}" 
                   class="w-full bg-green-600 hover:bg-green-700 text-white font-medium py-3 px-4 rounded-lg transition-colors inline-block text-center">
                    🎮 Ver Demo Internal Controllers
                </a>
            </div>
        </div>
    </div>

    <!-- Architecture Info -->
    <div class="mt-12 bg-gray-50 rounded-lg p-8">
        <h3 class="text-2xl font-bold text-gray-800 mb-6">
            🏗️ Arquitectura del Sistema
        </h3>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <div>
                <h4 class="text-lg font-semibold text-gray-700 mb-3">📂 Estructura de Controllers</h4>
                <div class="bg-white rounded-lg p-4 text-sm font-mono text-gray-600">
                    <div>assets/controllers/</div>
                    <div>├── internal/</div>
                    <div>│   ├── default/</div>
                    <div>│   ├── {{ tenant_info.subdomain ?? 'melisahospital' }}/</div>
                    <div>│   └── ...</div>
                    <div>└── apiplatform/</div>
                    <div>&nbsp;&nbsp;&nbsp;&nbsp;├── default/</div>
                    <div>&nbsp;&nbsp;&nbsp;&nbsp;├── {{ tenant_info.subdomain ?? 'melisahospital' }}/</div>
                    <div>&nbsp;&nbsp;&nbsp;&nbsp;└── ...</div>
                </div>
            </div>
            
            <div>
                <h4 class="text-lg font-semibold text-gray-700 mb-3">⚙️ Sistema de Fallback</h4>
                <div class="space-y-3">
                    <div class="flex items-center">
                        <span class="w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">1</span>
                        <span class="text-gray-700">Detectar subdomain actual</span>
                    </div>
                    <div class="flex items-center">
                        <span class="w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">2</span>
                        <span class="text-gray-700">Buscar controller específico</span>
                    </div>
                    <div class="flex items-center">
                        <span class="w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">3</span>
                        <span class="text-gray-700">Fallback a controller default</span>
                    </div>
                    <div class="flex items-center">
                        <span class="w-8 h-8 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-sm font-bold mr-3">4</span>
                        <span class="text-gray-700">Error si no existe ninguno</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Links -->
    <div class="mt-8 bg-blue-50 border border-blue-200 rounded-lg p-6">
        <h3 class="text-lg font-semibold text-blue-800 mb-4">
            🔗 Enlaces Rápidos
        </h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <a href="/api/docs" class="flex items-center p-3 bg-white rounded-lg hover:shadow-md transition-shadow">
                <span class="text-2xl mr-3">📖</span>
                <div>
                    <div class="font-semibold text-gray-800">API Docs</div>
                    <div class="text-sm text-gray-600">Documentación interactiva</div>
                </div>
            </a>
            <a href="/dashboard" class="flex items-center p-3 bg-white rounded-lg hover:shadow-md transition-shadow">
                <span class="text-2xl mr-3">🏥</span>
                <div>
                    <div class="font-semibold text-gray-800">Dashboard</div>
                    <div class="text-sm text-gray-600">Panel principal</div>
                </div>
            </a>
            <a href="/login" class="flex items-center p-3 bg-white rounded-lg hover:shadow-md transition-shadow">
                <span class="text-2xl mr-3">🔐</span>
                <div>
                    <div class="font-semibold text-gray-800">Login</div>
                    <div class="text-sm text-gray-600">Autenticación</div>
                </div>
            </a>
        </div>
    </div>

</div>
{% endblock %}