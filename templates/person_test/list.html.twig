{% extends 'base.html.twig' %}

{% block title %}Lista de Personas - Test Permisos{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">
                        <i class="fas fa-users"></i> Lista de Personas - Test de Permisos
                    </h3>
                    <small class="text-white-50">Sistema de permisos granulares con PermissionVoter</small>
                </div>
                <div class="card-body">
                    {% if persons is empty %}
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle"></i>
                            No hay personas en la base de datos.
                        </div>
                    {% else %}
                        <table class="table table-striped table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>ID</th>
                                    <th>Nombre</th>
                                    <th>Email</th>
                                    <th>Teléfono</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for person in persons %}
                                    <tr>
                                        <td>{{ person.id }}</td>
                                        <td>{{ person.name }} {{ person.lastName }}</td>
                                        <td>{{ person.email ?? 'N/A' }}</td>
                                        <td>{{ person.mobilePhone ?? 'N/A' }}</td>
                                        <td>
                                            <a href="{{ path('person_test_show', {id: person.id}) }}" 
                                               class="btn btn-sm btn-info">
                                                <i class="fas fa-eye"></i> Ver
                                            </a>
                                            <a href="{{ path('person_test_manual_check', {id: person.id}) }}" 
                                               class="btn btn-sm btn-secondary">
                                                <i class="fas fa-check-circle"></i> Verificar
                                            </a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
