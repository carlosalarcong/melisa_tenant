{# WiClinic Custom Welcome Banner #}
<div class="welcome-banner wiclinic-theme">
    <div class="welcome-text">
        <h2>¡Hola Dr. {{ app.session.get('user_name')|default('Doctor') }}! 🩺</h2>
        <p>WiClinic - Su sistema médico inteligente. Hoy tiene <strong>18 pacientes</strong> agendados.</p>
    </div>
    <div class="welcome-illustration">
        <i class="fas fa-heartbeat"></i>
    </div>
</div>

<style>
    /* WiClinic Custom Theme */
    .welcome-banner.wiclinic-theme {
        background: linear-gradient(135deg, #00b4db 0%, #0083b0 100%);
        border-left: 5px solid #00d4ff;
    }
    
    .welcome-banner.wiclinic-theme .welcome-text h2 {
        font-size: 1.85rem;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
    }
    
    .welcome-banner.wiclinic-theme .welcome-text p strong {
        font-weight: 800;
        color: #ffeb3b;
    }
    
    .welcome-banner.wiclinic-theme .welcome-illustration {
        font-size: 6rem;
        opacity: 0.25;
        animation: pulse 2s ease-in-out infinite;
    }
    
    @keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.05); }
    }
</style>
